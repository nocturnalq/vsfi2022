apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus.patchka.vsfi:9090
    basicAuth: false
    isDefault: true
    editable: true